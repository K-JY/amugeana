<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="amugeana">
	<select id="selectTypeFirstList" parameterType="hashmap" resultType="hashmap"> 
		SELECT TYPE_CD
		      ,TYPE_CATEGORY
		      ,PARENT_CATEGORY
		  FROM TB_CATEGORYMAP
		 WHERE PARENT_CATEGORY = 'CT001'
	  ORDER BY IDX ASC
	</select>
	
	<select id="selectTypeList" parameterType="hashmap" resultType="hashmap"> 
		SELECT TYPE_CD
		      ,TYPE_CATEGORY
		      ,PARENT_CATEGORY
		  FROM TB_CATEGORYMAP
		 WHERE PARENT_CATEGORY = ${category}
	  ORDER BY IDX ASC
	</select>

</mapper>

